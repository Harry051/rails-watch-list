<p><%= @list.name %></p>
<br>
<p><%= link_to "Add movie to list", new_list_bookmark_path(@list) %></p>
<br>
<ul>
  <% @list.bookmarks.each do |b| %>
    <li><%= b.comment %><%= link_to "    ❎", bookmark_path(b), data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this?"} %><li>
    <%=  %>
  <% end %>
</ul>
<br>
<p><%= link_to "🔙", lists_path %></p>
